apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "url-checker-helm.fullname" . }}-test-connection"
  labels:
    {{- include "url-checker-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "url-checker-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
